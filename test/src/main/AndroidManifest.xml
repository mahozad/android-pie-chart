<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="chart.test">

  <application android:theme="@style/TestAppTheme">
    <activity android:name="chart.test.ShowCaseMainActivity" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".ShowcaseComposeActivity" />
    <activity android:name=".ShowcaseViewActivity" />
  </application>

</manifest>
